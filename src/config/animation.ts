/** Animation feel knobs. Placeholder numbers, picked by eye — Craig's tuning pass. */
export const ANIMATION = {
  /** Crossfade duration between any two of the five clips, in seconds. */
  crossfadeSeconds: 0.25,
} as const;
